let count = 10;

for (count; count > 5; count--) {
  console.log(count);
}

// In for loop,
// The first declaration is initialization
// The second declaration is condition, Executes only if it returns true
// The third declaration is what we need to do after some operation executes for the next iteration.
